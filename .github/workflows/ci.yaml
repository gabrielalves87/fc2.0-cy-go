name: ci-golang-workflow
on: [push]
jobs:
  check-applications:
    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@v2
      - uses: action/setup-go@v2
        with: 
          go-version: 1.20
      - run : go test
      - run : go run match.go